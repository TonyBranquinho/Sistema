using Konscious.Security.Cryptography;
using System.Text;

namespace Sistema.service
{
    public static class VerificadorArgon2
    {
        public static bool Validar(string senhaDigitada, string senhaDoBanco)
        {
            // Aqui vai a lógica da biblioteca Konscious para comparar os hashes
            // Por enquanto, para o seu código compilar, ele precisa existir assim:
            return true;
        }
    }
}
